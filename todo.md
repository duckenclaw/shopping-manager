Amount controls
When adding a new item, search amongst previously added items. Only if not found show tag choice.
Remove places, just display everything in checkbox mode
Checkboxes for each item, when at least one is checked, show "Complete" button that removes all checked items.
Move Add button to the bottom of the screen